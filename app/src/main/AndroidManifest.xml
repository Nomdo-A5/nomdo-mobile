<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nomdoa5.nomdo">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Nomdo"
        tools:targetApi="n">
        <activity
            android:name=".ui.balance.MoneyReport"
            android:exported="true" />
        <activity
            android:name=".ui.auth.ForgetPassActivity3"
            android:exported="true" />
        <activity
            android:name=".ui.auth.ForgetPassActivity2"
            android:exported="true" />
        <activity
            android:name=".ui.auth.ForgetPassActivity"
            android:exported="true" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Nomdo.NoActionBar" />
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.Auth" />
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Auth" />
        <activity android:name=".ui.splash.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.Auth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>